# Template for rfid_secrets.yaml — copy to rfid_secrets.yaml and fill in.
# rfid_secrets.yaml is gitignored. Deep-merged on top of rfid_params.yaml at load time.
# Override path: set RFID_NODE_SECRETS env var to point at a custom copy.

spb_bridge:
  local:
    host:     localhost
    username: ""
    password: ""

  hivemq:
    host:     ""       # e.g. abc123.s1.eu.hivemq.cloud
    username: ""
    password: ""
